{
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": [
    "apps",
    "extensions",
    "infrastructure",
    "packages",
    "config",
    "scripts",
    "tests",
    ".eslintrc.cjs",
  ]
}

{
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": [
    "apps",
    "extensions",
    "infrastructure",
    "packages",
    "scripts",
    "tests",
    "tailor.config.js",
    ".eslintrc.cjs",
  ]
}
